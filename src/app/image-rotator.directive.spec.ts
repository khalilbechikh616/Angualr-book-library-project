import { ImageRotatorDirective } from './image-rotator.directive';

describe('ImageRotatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageRotatorDirective();
    expect(directive).toBeTruthy();
  });
});
